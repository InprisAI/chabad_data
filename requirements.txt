# Flask server
Flask==3.0.0
gunicorn==21.2.0
python-dotenv==1.0.0

# HTTP requests (for Grok API + GitHub downloads)
requests==2.31.0

# Fuzzy search
rapidfuzz==3.5.2

# OpenAI for semantic search
openai==1.12.0

# Data handling (for merge scripts only, not for server)
pandas==2.1.4
openpyxl==3.1.2
